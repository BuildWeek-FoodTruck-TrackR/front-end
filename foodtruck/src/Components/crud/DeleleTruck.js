import React from 'react';

import { connect } from 'react-redux';
import { deleteData } from '../../state/_shared/middleware/api';

const DeleteTruck = (props) => {
  
  

  return <>
  </>
}

export default connect(null, { deleteData })(DeleteTruck)